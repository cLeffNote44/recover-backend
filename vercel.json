{
  "buildCommand": "cd source && pnpm run build",
  "outputDirectory": "source/dist",
  "installCommand": "cd source && pnpm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
